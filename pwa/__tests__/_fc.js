'use strict';

// Resolves fast-check from the automation project's node_modules so the PWA
// pure-function tests can run via `node --test` without a separate install in
// this folder. Mirrors viewer/__tests__/_fc.js.
module.exports = require('D:\\DMM-Automation\\exercise_library_automation_v22_mouse_park_batch20\\node_modules\\fast-check');
